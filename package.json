{
  "name": "mockoon-api",
  "version": "1.0.0",
  "description": "Public mock API using Mockoon CLI",
  "main": "index.js",
  "scripts": {
    "start": "mockoon-cli start --data environment.json --port 3000 --disable-log"
  },
  "dependencies": {
    "@mockoon/cli": "^1.21.0"
  }
}
